;;; NOTE:
;;; The canonical implementation of the `machine_constants` module lives in core/.
;;; Keep this file as a thin forwarder to avoid duplicate, diverging definitions.

(include "core/machine_constants.scm")

